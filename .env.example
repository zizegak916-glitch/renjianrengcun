# Keep real values in Cloudflare Pages / GitHub Actions secrets. Never commit `.env`.
# Public browser variables must begin with PUBLIC_ and must contain no credentials.
PUBLIC_SITE_URL=https://the-fool.xyz

# Deployment only (set as GitHub repository secrets, not here)
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_ACCOUNT_ID=
